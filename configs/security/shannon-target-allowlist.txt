# Shannon target allowlist
# One host pattern per line, wildcard supported (bash-style), comments allowed.
#
# Examples:
# *.staging.mycompany.com
# api-staging.mycompany.com
# host.docker.internal
# localhost
# 127.0.0.1

localhost
127.0.0.1
host.docker.internal
*.run.app
*.staging.*
