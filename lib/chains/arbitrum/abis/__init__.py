"""Pinned Aave V3 / Arbitrum ABIs and the Arbiscan fetcher.

ABIs are checked into git (one JSON per role + a sibling `.meta.json` with
chain_id, source, hash, fetched_at). Refresh via :mod:`fetcher`.
"""
