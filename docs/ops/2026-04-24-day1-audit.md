# Day 1 Ops Audit — 2026-04-24

## Verified State

- Cloud SQL `control-plane-db` in `tho-ai-agent` was stopped with automated backups disabled.
- `agentic-pm-hub` is private; unauthenticated `/health` returns 403, authenticated `/health` returns 200.
- Inference proxy health returns all tiers healthy: Windows GPU, Pi rari1, Pi rari2, Mac local, and Kimi cloud.
- AIAster app containers were crash-looping while AIAster Postgres and Redis were healthy.
- OpenBB serves provider coverage at `/api/v1/coverage/providers`; the expected Sapphire compatibility path `/api/v1/openbb/providers` returned 404.
- Foundry sync is configured but uploads fail before first success with a mix of 404, 403, DNS, and TLS failures against the apply-action endpoint.
- Cloud Run warning-or-higher logs in the sampled one-hour window included `agentic-pm-hub` auth noise and `sapphire-analytics` warnings.

## Actions Taken

- Resumed Cloud SQL `control-plane-db` by setting activation policy to `ALWAYS`.
- Enabled daily automated backups at `21:00`.
- Created an on-demand recovery backup: `day1-operational-recovery-2026-04-24`.
- Decommissioned the three broken AIAster app containers by setting Docker restart policy to `no` and stopping them.

## Follow-Ups

- Verify a restore drill for the Cloud SQL backup in a non-production instance before marking backup/restore complete.
- Apply the updated OpenBB LaunchAgent template after this PR lands so `/api/v1/openbb/providers` becomes live.
- Get the correct Foundry ontology API name, upsert action API name, and service-account permission from the Foundry partner.
- Investigate `sapphire-analytics` warnings with a service-specific log pull.
