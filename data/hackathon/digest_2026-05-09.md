# Hackathon submission digest — 2026-05-09

## ⏱️ Countdowns
- 0G submission: T-7 days (deadline 2026-05-16 23:59 UTC+8 = 15:59 UTC)
- London registration: T-16 days (deadline 2026-05-25)
- London submission: T-36 days (deadline 2026-06-14)
- London results: T-39 days (2026-06-17)

## 🟪 0G APAC Hackathon (Track 2: Verifiable Finance)

| Item | Status | Notes |
|---|---|---|
| Engineering complete | ✓ done | `lib/og/`, plugin tools, deploy script, 56 tests |
| PR #525 merged | ✓ done | already on main |
| `npm install` in lib/og/_ts/ | ✗ not done | `cd lib/og/_ts && npm install` required before testnet deploy |
| Testnet deployed | ✗ not done | no deployments.json; README still has `0x...` placeholders |
| Mainnet deployed | ✗ Ari only | needs ~0.05 0G mainnet gas; run `python3 scripts/deploy_og_chain.py --network mainnet` |
| Mainnet signal published | ✗ Ari only | judges grade on real on-chain activity |
| Mainnet addresses in README | ✗ Ari only | replace `0x...` in `docs/hackathon-0g/README.md` after deploy |
| Demo recorded | ✗ Ari only | follow `docs/hackathon-0g/demo-script.md`, upload to YouTube |
| X post published | ✗ Ari only | draft ready at `docs/hackathon-0g/x-post.md` |
| HackQuest form submitted | ✗ Ari only | https://www.hackquest.io |

**0G deploy order of operations (all blocking each other in sequence):**
`npm install` → testnet deploy + smoke → mainnet deploy → publish signal → paste addresses → record demo → post X → submit form

## 🟦 Arbitrum London Buildathon (Best Agentic Project)

| Item | Status | Notes |
|---|---|---|
| `lib/hackathon/sentinel.py` | ✓ done | on main |
| `contracts/SapphireSentinelRegistry.sol` | ✓ done | on main |
| Lane A: `privacy_mock.py` | ✓ done | `lib/hackathon/privacy_mock.py` present on main |
| Lane B: `chain_health_gate.py` | ✓ done | `lib/hackathon/chain_health_gate.py` present on main |
| Lane C: `hackathon_smoke.sh` | ✓ done | `scripts/hackathon_smoke.sh` present on main |
| Lane D: docs cross-pollination | ✓ done | `docs/hackathon/london-demo-script.md` + `sapphire-sentinel-london-2026.md` on main |
| Robinhood Chain testnet deployed | ✗ not done | `data/chain/deployments.json` absent; run `python3 scripts/deploy_robinhood_chain.py` |
| Demo recorded | ✗ Ari only | use `docs/hackathon/london-demo-script.md` |
| HackQuest registered (deadline May 25) | ✗ Ari only | T-16 days — register before submission opens |
| HackQuest submitted (deadline June 14) | ✗ Ari only | T-36 days |

## 🎯 Most-urgent action for Ari today

**Run `cd lib/og/_ts && npm install` then `python3 scripts/deploy_og_chain.py --network testnet` (and mainnet immediately after) — T-7 days and the entire 0G submission chain (signal, addresses, demo, X post, form) is blocked on this single step.**

## 🛠️ Engineering items currently autonomous-doable

1. **Robinhood Chain testnet deploy prep** — `scripts/deploy_robinhood_chain.py` exists; I can audit it for any missing config or environment vars and open a PR with any fixes needed so the script runs cleanly without Ari debugging it on submission day.
2. **Cross-chain Sentinel smoke test** — I can write or extend `scripts/hackathon_smoke.sh` to cover both the 0G and Robinhood Chain deploy paths in one pass, reducing demo-day friction.
3. **npm install readiness check** — I can read `lib/og/_ts/package.json` and verify the dependency set is pinned and complete, flagging any version drift before Ari runs the install.
