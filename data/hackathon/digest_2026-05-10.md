# Hackathon submission digest — 2026-05-10

## ⏱️ Countdowns
- 0G submission: T-6 days (deadline 2026-05-16 23:59 UTC+8 = 15:59 UTC)
- London registration: T-15 days (deadline 2026-05-25)
- London submission: T-35 days (deadline 2026-06-14)
- London results announce: T-38 days (2026-06-17)

## 🟪 0G APAC Hackathon (Track 2: Verifiable Finance)

| Item | Status | Notes |
|---|---|---|
| Engineering complete | ✓ done | `lib/og/`, plugin tools, deploy script, 56 tests |
| PR #525 merged | ✓ done | already on main |
| `npm install` in lib/og/_ts/ | ✗ not done | `cd lib/og/_ts && npm install` required before testnet deploy |
| Testnet deployed | ✗ not done | `data/chain/deployments.json` absent — run `python3 scripts/deploy_og_chain.py --network testnet` |
| Mainnet deployed | ✗ Ari only | needs ~0.05 0G mainnet gas; run `python3 scripts/deploy_og_chain.py --network mainnet` |
| Mainnet signal published | ✗ Ari only | judges grade on real on-chain activity |
| Mainnet addresses in README | ✗ Ari only | `0x...` placeholders still in `docs/hackathon-0g/README.md` — replace after deploy |
| Demo recorded | ✗ Ari only | follow `docs/hackathon-0g/demo-script.md`, upload to YouTube |
| X post published | ✗ Ari only | draft ready at `docs/hackathon-0g/x-post.md` |
| HackQuest form submitted | ✗ Ari only | https://www.hackquest.io |

**0G deploy order of operations (each step gates the next):**
`npm install` → testnet deploy + smoke → mainnet deploy → publish signal → paste addresses → record demo → post X → submit form

## 🟦 Arbitrum London Buildathon (Best Agentic Project)

| Item | Status | Notes |
|---|---|---|
| `lib/hackathon/sentinel.py` | ✓ done | on main |
| `contracts/SapphireSentinelRegistry.sol` | ✓ done | on main, ABI in `data/chain/` |
| Lane A: `privacy_mock.py` | ✓ done | `lib/hackathon/privacy_mock.py` present on main |
| Lane B: `chain_health_gate.py` | ✓ done | `lib/hackathon/chain_health_gate.py` present on main |
| Lane C: `hackathon_smoke.sh` | ✓ done | `scripts/hackathon_smoke.sh` present on main |
| Lane D: docs cross-pollination | ✓ done | `docs/hackathon/sapphire-sentinel-london-2026.md` + `london-demo-script.md` on main |
| Robinhood Chain testnet deployed | ✗ not done | `data/chain/deployments.json` absent — run `python3 scripts/deploy_robinhood_chain.py` |
| Forge test suite | ✓ done | 18-case Forge suite (chain ID 46630) on main |
| Demo recorded | ✗ Ari only | use `docs/hackathon/london-demo-script.md` |
| HackQuest registered (deadline May 25) | ✗ Ari only | T-15 days — register before submission window opens |
| HackQuest submitted (deadline June 14) | ✗ Ari only | T-35 days |

## 🎯 Most-urgent action for Ari today
**T-6 days and shrinking — run `cd lib/og/_ts && npm install` then `python3 scripts/deploy_og_chain.py --network testnet` immediately, followed by `--network mainnet` with a funded wallet.** The entire 0G submission chain (signal publish → addresses in README → demo recording → X post → HackQuest form) is serially blocked on this single `npm install` step. After T-6 days there is no buffer left for any slip.

## 🛠️ Engineering items currently autonomous-doable
1. **Audit `lib/og/_ts/package.json` for dependency pin completeness** — verify all deps are pinned to exact versions and flag any `^`/`~` ranges that could cause `npm install` to pull breaking changes on demo day; open a PR with any fixes before Ari runs the install.
2. **Robinhood Chain deploy script pre-flight** — read `scripts/deploy_robinhood_chain.py` and verify all required env vars / config keys are documented; open a PR adding a `--dry-run` / preflight-check flag so the script self-validates the wallet balance and RPC before spending gas.
3. **Extend `scripts/hackathon_smoke.sh` to cover the Robinhood Chain path** — currently smoke tests the 0G flow; adding a parallel Robinhood Chain testnet block would let Ari validate both submissions with one command on demo day.

---
_Generated by [Claude Code](https://claude.ai/code/session_016Fu8QcktAShteZuzMTzmAH)_
