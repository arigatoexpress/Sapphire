# Pinned dep set for CI `make test` and `make test-plugin`.
#
# Scope: everything the unit test suite imports, pinned tightly enough that
# pip doesn't walk the universe trying to resolve. Pins mirror
# services/alpha/requirements.txt where they overlap.
#
# This is NOT a runtime lockfile — services each own their own requirements.
# Update this when a new test ModuleNotFoundError lands in CI.

# test runner
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0

# web / http
aiohttp==3.13.5
fastapi==0.136.0
flask==3.1.3
httpx==0.28.1
requests==2.33.1
websockets>=13.0,<17

# data / validation
pydantic==2.13.2
python-dateutil==2.9.0.post0
orjson==3.10.13
pyyaml==6.0.3
redis==5.2.1

# scientific
numpy==2.4.4
pandas==2.2.3
scipy==1.15.0
scikit-learn==1.6.0

# logging / config
loguru==0.7.3
python-dotenv==1.0.1

# google / firebase — pinned tightly; without these pip resolution explodes
google-cloud-firestore==2.27.0
google-cloud-pubsub==2.37.0
google-cloud-storage==2.19.0
google-generativeai==0.8.4
firebase-admin==6.9.0

# crypto / chain
web3>=7.0,<8
eth-account>=0.13,<1
cryptography==44.0.0
certifi==2026.2.25
