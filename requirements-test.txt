# Pinned dep set for CI `make test` and `make test-plugin`.
#
# Scope: everything the unit test suite imports, pinned tightly enough that
# pip doesn't walk the universe trying to resolve. Pins mirror
# services/alpha/requirements.txt where they overlap.
#
# This is NOT a runtime lockfile — services each own their own requirements.
# Update this when a new test ModuleNotFoundError lands in CI.

# test runner
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0

# property-based + mutation testing (Tranche 6 Lane 1 — see
# docs/products/test-rigor-0.1.0.md). Authorized for the test-rigor lane only;
# do not leak into runtime requirements.
hypothesis>=6.152.7,<7
# mutmut 3.x dropped the glob2 transitive (glob2 setup.py imports
# distutils.command.install.install_layout, removed in setuptools 74+, which
# bricks `pip install -r requirements-test.txt` in clean cloud envs and
# blocks flask/pandas/reportlab from installing — see issue #539).
mutmut>=3.5.0,<4

# web / http
aiohttp==3.14.0
annotated-doc==0.0.4
fastapi==0.136.3
# feedparser intentionally omitted: lib/macro/feedparser_compat.py falls back
# to stdlib XML parsing when feedparser/sgmllib is missing. Including
# feedparser==6.0.11 here pulled sgmllib3k, an unmaintained 2009 SGML backport
# whose setup.py also breaks on setuptools 74+ — see issue #539.
flask==3.1.3
httpx==0.28.1
requests==2.34.2
websockets>=16.0,<17

# data / validation
pydantic==2.13.4
python-dateutil==2.9.0.post0
orjson==3.11.9
pyyaml==6.0.3
redis==7.4.0

# scientific
numpy==2.4.4
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.8.0

# reporting / pdf — pulled in by lib/research_notes (added by 2026-04-28
# `feat(research): LP-grade research notes pipeline 0.1.0`)
reportlab>=4.0

# logging / config
loguru==0.7.3
python-dotenv==1.2.2

# google / firebase — pinned tightly; without these pip resolution explodes
google-cloud-firestore==2.27.0
google-cloud-pubsub==2.38.0
google-cloud-storage==3.10.1
google-generativeai==0.8.6
firebase-admin==6.9.0

# crypto / chain
web3>=7.0,<8
eth-account>=0.13.7,<1
cryptography==48.0.0
certifi==2026.4.22

# webauthn admin scaffold (services/analytics_dashboard/auth)
webauthn==2.7.1
itsdangerous==2.2.0
